<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <Button
        android:id="@+id/showDialogButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="显示对话框"
        android:onClick="showSimpleMessageDialog"/>

    <Button
        android:id="@+id/showListDialogButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="显示列表对话框"
        android:layout_marginTop="16dp"
        android:onClick="showListSelectionDialog"/>

    <Button
        android:id="@+id/showCustomDialogButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="显示自定义对话框"
        android:layout_marginTop="16dp"
        android:onClick="showCustomViewDialog"/>

</LinearLayout> 